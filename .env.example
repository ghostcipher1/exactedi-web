# Public site URL (canonical links, sitemap, JSON-LD)
VITE_SITE_URL=https://exactedi.com

# Google Tag Manager (injected into index.html at build). Must match tagmanager.google.com install snippet.
VITE_GTM_ID=GTM-522ZBZWZ
# Set to false on local/preview to skip dataLayer events (GTM still loads from index.html unless you remove it)
# VITE_ENABLE_ANALYTICS=false
# GA4 Measurement ID (G-XXXXXXXX) is configured in GTM, not here — see ANALYTICS_SETUP.md

# Vercel serverless lead capture (set in Vercel dashboard, not needed locally)
# RESEND_API_KEY=
# LEADS_TO_EMAIL=contact@exactedi.com
# RESEND_FROM_EMAIL=ExactEDI <onboarding@resend.dev>
# LEADS_WEBHOOK_URL=

# Supabase (blog — optional until you migrate to local MDX)
VITE_PUBLIC_SUPABASE_URL=
VITE_PUBLIC_SUPABASE_ANON_KEY=
